# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / tooling
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/

# Secrets — never commit rail credentials
.env
.env.*
!.env.example

# Editors / OS
.vscode/
.idea/
.DS_Store

# Manim
.venv-manim/
video/media/
video/out/
video/tools/trace/

# Generated narration. The manifest stays: it is what fit_pace.py solves the
# scene timings against, so the SEGMENTS in each scene file are explicable
# without the 25MB of audio that produced them.
video/audio/*.wav
video/audio/s[0-9]/

