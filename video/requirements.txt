# The video toolchain only. The project itself needs none of this.
#
#   python3 -m venv --system-site-packages .venv-manim
#   .venv-manim/bin/pip install -r video/requirements.txt
#
# System packages first (Debian/Ubuntu):
#   sudo apt install -y libpango1.0-dev libcairo2-dev pkg-config ffmpeg
manim>=0.19
